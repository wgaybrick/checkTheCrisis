<!DOCTYPE html>
<html>
	<head>
  	<title><%= title %></title>
  	<%= javascript_include_tag 'jquery-1.5', 'jquery.rails', 'application' %>
  	<%= csrf_meta_tag %>
		<%= render 'layouts/stylesheets' %>
	</head>
	<body>
		<div class="container">
			<%= render 'layouts/header' %>
						
			<section>
				<%= yield %>
			</section>	
			
			<%= render 'layouts/footer' %>
		</div>
		
	</body>
</html>
